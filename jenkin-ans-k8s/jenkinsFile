pipeline {
  agent {
    docker { image 'node:16-alpine' }
  }
  stages {
    stage('Git-checkout') {
      steps {
        sh 'echo passed'
      }
    }
    stage('maven build') {
      steps {
        sh 'cd jenkin-ans-k8s && mvn clean package'
        echo 'maven build works!'
      }
    }
  }
}

