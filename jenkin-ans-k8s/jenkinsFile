pipeline {
  agent {
    docker { image 'maven' }
  }
  stages {
    stage('Git-checkout') {
      steps {
        sh 'echo passed'
      }
    }
    stage('maven build') {
      steps {
        sh 'cd jenkin-ans-k8s && mvn clean package'
        echo 'maven build works!'
      }
    }
  }
}

